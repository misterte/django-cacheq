============
Installation
============

At the command line::

    $ easy_install django-cacheq

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cacheq
    $ pip install django-cacheq
